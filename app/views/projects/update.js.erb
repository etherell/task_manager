<% if @project.errors.empty? %>
  $(".project-title-<%= @project.id %>").html("<%= j render 'project_title', project: @project %>");
<% else %>
  $('.edit-project-form-<%= @project.id %>').addClass('is-invalid');
<% end %>
<%= render partial: 'shared/toaster' %>
