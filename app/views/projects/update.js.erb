<% if @project.errors.empty? %>
  $(".project-body-<%= @project.id %>").html("<%= j render 'projects/body', project: @project %>");
<% else %>
  $('.edit-project-title-<%= @project.id %>').addClass('is-invalid');
<% end %>
<%= render partial: 'shared/toastr' %>
